spring:
  application:
    name: Tennis-Pro
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: false
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never